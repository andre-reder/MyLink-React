import { domain } from './enviromentVariables';

export const pathName = `https://rotaslink${domain}/api`;
export default pathName;

// novo servidor alterar domain no env e homepage no package.json, homolog.
